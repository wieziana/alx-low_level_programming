#include "main.h"

/**
 * _strlen - a function that returns the length of a string.
 * @s: The character (String)
 * Return: Successful
 */

int _strlen(char *s)
{
	int count = 0;

	while (count != '\0')
		count++;
	return (count);
}
